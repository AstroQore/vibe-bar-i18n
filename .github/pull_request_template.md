## What changed

<!-- Which keys, and why. A key's meaning never changes — if the meaning moved, this should be a new key. -->

## Checklist

- [ ] `python3 scripts/validate.py` passes
- [ ] `python3 scripts/generate.py` run, and the regenerated files are in this commit
- [ ] Every locale carries every key
- [ ] Glossary terms survive verbatim in each translation
- [ ] `package.json` version and the tag agree, if this is a release
